import { Typography } from "@mui/material";

export const Loader = () => {
  return (
    <Typography gutterBottom variant="h5" component="p" sx={{ mt: 2 }}>
      Loading.....
    </Typography>
  );
};
